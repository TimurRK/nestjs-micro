{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "author": "t.kosminov",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.build.json",
    "start": "ts-node --files -r tsconfig-paths/register src/main.ts",
    "start:build": "NODE_ENV=development node -r ts-node/register -r tsconfig-paths/register dist/api/src/main.js",
    "start:dev": "nodemon"
  }
}
